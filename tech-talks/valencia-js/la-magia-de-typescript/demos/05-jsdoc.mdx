# JSDoc

```js
/**
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
function sum(a, b) {
  return a + b;
}
```

## No chiva errores

```js
/**
 * @param {number} a
 * @param {number} b
 * @returns {`test-${number}`}
 */
function sum(a, b) {
  return a + b;
}
```

## Copia y pega en consola

```js
/**
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
function sum(a, b) {
  return a + b;
}
```